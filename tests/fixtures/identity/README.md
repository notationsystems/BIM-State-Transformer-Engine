# Historical IFC identity fixtures

Generated before the portable-import patch, using main
`2493bdadcbcd7f38c1a4569cc2bfc7eba5ca9a14`, Python 3.12.14 / NumPy 2.3.5,
from `gat/demo/model.ifc` through `GatSession.from_text` with the fixed source
locator `legacy-v1-model.ifc`. The world digest is:

`f628952eaff3bac72edf1705da3d66e196bb6ee2736382535bd3f3c33a73a2ad`

The snapshot and genesis ledger are verification fixtures, not newly generated
expected output from the revised importer. Do not regenerate them to make a
changed identity algorithm pass. They contain synthetic demo data only.
